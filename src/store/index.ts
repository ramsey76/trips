import Vue from 'vue';
import Vuex from 'vuex';
import {authenticationStore} from './Authentication';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    authenticationStore
  },
});
